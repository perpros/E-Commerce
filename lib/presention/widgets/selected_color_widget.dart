import 'package:flutter/material.dart';

class SelectedColorWidget extends StatelessWidget{
  const SelectedColorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}